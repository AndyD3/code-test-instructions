# Front End for URL shortener

TBD - skeleton for now 
This will retrieve and display urls from backend to prove apps run and connecting

## Steps to run
1. Build the project using
   `npm install`
2. Run using `npm run dev`
3. The web application is accessible via http://localhost:5173/